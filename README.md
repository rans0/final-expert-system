### Installation

```bash
  $ git clone https://github.com/rans0/nisuma-paper.git
  $ cd nisuma-paper
  $ composer install
  $ composer require laravel/ui
  $ npm install
  $ php artisan key:generate
  $ php artisan migrate:fresh --seed
```
